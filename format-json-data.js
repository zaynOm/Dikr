// import fs from 'fs';
// import data from './assets/data/hisn.json' with { type: 'json' };

// const formatedData = Object.keys(data).map((key) => ({
//   title: key,
//   ...data[key],
// }));

// fs.writeFileSync('./assets/data/data.json', JSON.stringify(formatedData));

import data from './src/assets/data/data.json';

const formatedData = data.map((item) => {});
